import SwiftUI

enum AppTheme {
    /// Primary orange from AR Assist mockups.
    static let orange = Color(red: 1.0, green: 0.45, blue: 0.0)
    static let orangeDeep = Color(red: 0.95, green: 0.35, blue: 0.0)
    static let panel = Color.white.opacity(0.08)
    static let panelStroke = Color.white.opacity(0.14)
    static let statusGreen = Color(red: 0.2, green: 0.85, blue: 0.35)
}
