---
description: GGSApple — Instant iOS, expert-web, GGS Mac Mini runtime
alwaysApply: true
---

# GGSApple agent rules

**Full playbook:** Obsidian MCP → `Punctualities/Projects/GGSApple` + `Company/GGS/Remote-AR-Support` + `Projects/GGSApple/Agent-Playbook`

## Identify & read (Obsidian MCP)

1. `Projects/GGSApple/GGSApple` — current focus
2. `Projects/GGSApple/Connectivity` + `Company/GGS/Remote-AR-Support` — live URLs
3. `search-vault` — recent Progress; prefer newest `updated:` dates (check `Company/GGS/` if product notes look stale)

## Two repos (both on `main`)

| Repo | Path | Branch | Notes |
|------|------|--------|-------|
| Monorepo | repo root | **`main`** | iOS + backend + compose |
| Expert web | `expert-web/` | **`main`** | Nested `.git` — separate push |

`experiment/web-expert` is merged — do not treat it as current.

## Live vault updates (mandatory)

When you change URLs, runtime host, working branch, or product priority → edit Connectivity / Remote-AR-Support / hub / Punctualities **in the same turn**. Do not wait for session end. See vault `Punctualities/Vault-Logging`.

## Git identity (Vercel blocks wrong email)

```
Name:  Syed Mohammad Anas
Email: 166297310+SyedMohammadAnas@users.noreply.github.com
```

Use `/usr/bin/git`. No heredoc commits. Vault: `Punctualities/Terminal-macOS`.

## Runtime — GGS Mac Mini (CURRENT)

```bash
ssh ggs-macmini
export PATH=/opt/podman/bin:$PATH
cd "/Users/rameshbabu/anassyed/GGS Remote AR Support/GGSApple"
podman compose -f compose.podman.yml ps
curl -s -m 15 https://ggs-macmini.tail3bc01f.ts.net:8443/health
```

| Service | URL |
|---------|-----|
| Expert | `https://ggs-macmini.tail3bc01f.ts.net` |
| API | `https://ggs-macmini.tail3bc01f.ts.net:8443` |
| LiveKit | `wss://ggs-macmini.tail3bc01f.ts.net:7880` (tailnet Serve) |

Homelab LiveKit/API = **legacy**. Vercel `ggsexpert.vercel.app` = **backup**.

## Expert-web

- Primary lab: Mac Mini Funnel / Podman
- Backup: push `GGSApple-expert-web` `main` → Vercel
- Env: log **names only** in vault
- Code: `expert-web/` — not `backend/public/expert/`

## Product constraints

- **Customer** = iOS native (`ios-app/`)
- **Expert** = Next.js web
- iOS AR models = **USDZ**
- Full AR media needs Tailscale on the phone
- Secrets: `Secrets/Projects/GGSApple-Secrets` — never paste values

## After session (Obsidian MCP)

1. `Projects/GGSApple/Progress/<topic>-YYYY-MM-DD.md`
2. `Parent: [[Projects/GGSApple/Runs/<Topic-Hub>]]` — one link only
3. Topic hub latest line (plain text)
4. Reusable lesson? → `Punctualities/Projects/GGSApple` + mirror this file if critical
